# Flooded rice

Due to their unique hydrology, rice paddies go through a few dedicated paths during the simulation which alter the usual model behaviour around irrigation and soil properties.

Each simulation day, Rice paddies are recognized as all {ref}`cells <simulation-unit>` in which a crop whose {ref}`CN class <CN-class>` is 7 is currently growing.

